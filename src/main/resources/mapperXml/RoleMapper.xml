<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sise.makerSpace.dao.RoleDao">

    <insert id="addSomeOnesRole">
        insert into user_role(user_id,role_id,handling_time) values (#{applicant_id},#{role_id},now())
    </insert>

</mapper>